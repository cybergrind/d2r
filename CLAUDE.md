@AGENTS.md

Claude-specific: the auto-memory for this project (`~/.claude/projects/-mnt-extra-1000-games-d2r/memory/`) carries the same rules plus session history; AGENTS.md is the shared source of truth for both Claude and Codex — edit it, not just memory, when a rule changes.
